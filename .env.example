# Local dev (SQLite)
DATABASE_URL=sqlite:///./alocks.db

# Production (PostgreSQL on Railway) — uncomment and fill in:
# DATABASE_URL=postgresql://username:password@host:port/railway

CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
ENV=dev
